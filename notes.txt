/**
 * Rota / Recurso
 */

/*
    Métodos HTTP:

    GET: Buscar/listar uma infomação do back-end
    POST: Criar uma informação no back-end
    PUT: Alterar uma informação no back-end
    DELETE: Deletar uma informação no back-end
*/

/**
 * Tipos de parâmetros:
 * 
 * Query: Parâmentos nomeados enviados na rota após "?" (filtros, paginação)
 * Route: Parâmetros utilizados para identificar recursos (busca algo especifico dentro ad rota, como um usuario)
 * Request Budy: Corpo da requisição. utilizado para criar ou alterar recursos
 * 
 */

 /** Bancos de dados:
  * SQL: MySQL, SQLite, PostgreSQL, Oracle, Microsoft SQL Server
  * NoSQL: MongoDB, CouchDB, etc
  */

  /**
   * Driver: SELECT * FROM users
   * Query Builder: table('users').select('*').where()
   */
   
  Deploy (Hospedagem em servidores)
  Back end - Heroku
  Front end - netlify
  Mobile - App para android (google play) Ver como criar app usando expo no rocketset